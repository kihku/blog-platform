'use client';

import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        secondary: {
          main: '#ffba00',
        },
        primary: {
          main: '#210440'
        },
        info: {
          main: '#e5958e'
        }
      },
    status: {
    },
  });
export default theme;